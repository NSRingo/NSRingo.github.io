import { ModuleInstall } from '@components'
import _Footer from './_Footer.md'
import PrivateRelay from '../../wiki/private-relay/Home.md'

# 🌐 专用代理
:::danger ‼️ 已失效
⚠️ 从 iOS 17.? 开始，「iCloud 专用代理」激活所使用的`mask-api.icloud.com`，不再可以通过 MitM 改写请求。
⚠️ 此模块已停止更新。
:::
<PrivateRelay />
<_Footer />
